\documentclass[11pt]{article}
\usepackage{rldm25}
\usepackage{palatino}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage[T1]{fontenc}
\usepackage{lmodern}  % Fuente moderna con soporte completo
\renewcommand{\bfdefault}{b} % Asegura que las negritas se activen


\title{$title$}

\author{
$for(by-author)$
  $by-author.name.literal$ % Nombre del autor
  $if(by-author.affiliations)$
    \\ $for(by-author.affiliations)$
      $it.id$ % Afiliación
      \\ $if(it.address)$
        $it.address$ % Dirección
      $endif$
    $endfor$
  $endif$
  $if(by-author.email)$
    \\ \texttt{$by-author.email$} % Email
  $endif$
  $if(by-author.last)$
    \AND
  $else$
    \And
  $endif$
$endfor$
}


\begin{document}

\maketitle

\begin{abstract}
$abstract$
\end{abstract}

\keywords{$for(keywords)$$keywords$$sep$, $endfor$}

$body$

\end{document}
